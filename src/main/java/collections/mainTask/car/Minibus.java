package collections.mainTask.car;

public class Minibus extends Van {

    public Minibus(int id, String model, int cost, double fuelConsumption, int maxSpeed, int productionYear, int passengerSeats, TypeOfBody typeOfBody) {
        super(id, model, cost, fuelConsumption, maxSpeed, productionYear, passengerSeats, typeOfBody);
    }
}

